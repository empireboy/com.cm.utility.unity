using UnityEngine;

namespace CM.Events
{
	/// <summary>
	/// A delegate with a Color parameter.
	/// </summary>
	/// <param name="value">A Color parameter.</param>
	public delegate void ColorEvent(Color value);
}